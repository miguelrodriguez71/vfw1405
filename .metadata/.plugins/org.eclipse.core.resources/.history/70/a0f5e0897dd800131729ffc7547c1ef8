Ti.UI.setBackgroundColor("#000");

var mainScreen = Ti.UI.createWindow({
	backgroundColor : "blue"
});

var labelView = Ti.UI.createView({
    backgroundColor : "#fff",
    height : 60,
    left : 20,
    right : 20,
    top : 40,
    zindex : 0
	
});

var nextView = Ti.UI.createView({
	backgroundColor : "#fff",
	top : 60 + labelView.height + 20,
	botton :20,
	left : 80,
	right : 20,
	widht : 20,
	height : 60
	zindex : 1
	
});



mainScreen.add(labelView, nextView);
mainScreen.open();

